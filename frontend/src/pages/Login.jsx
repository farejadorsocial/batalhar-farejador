import{useState}from'react';import{Link,useNavigate}from'react-router-dom';import{login}from'../api/client';export default function Login({onAuth}){const[e,setE]=useState(''),[p,setP]=useState(''),[err,setErr]=useState(''),nav=useNavigate();async function go(x){x.preventDefault();try{onAuth(await login(e,p));nav('/')}catch(a){setErr(a.message)}}return <Auth title="Entrar na arena"><form onSubmit={go}><label>E-mail<input type="email" value={e} onChange={x=>setE(x.target.value)} required/></label><label>Senha<input type="password" value={p} onChange={x=>setP(x.target.value)} required/></label>{err&&<div className="error">{err}</div>}<button className="primary">Entrar</button><p>Não possui conta? <Link to="/criar-conta">Criar conta</Link></p></form></Auth>};function Auth({title,children}){return <div className="auth"><section><div className="brand big">🔎 BATALHA <b>DE FAREJADOR</b></div><h1>{title}</h1><p className="muted">Sua conta, seus torneios, seu progresso.</p>{children}</section></div>}
